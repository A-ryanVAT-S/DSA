#include <bits/stdc++.h>
using namespace std;
//To access map or set last element:
#define ll long long
set<ll> arr;
auto lastElement = arr.rbegin(); // it returns a reverse iterator to the last element of the set
// auto lastElement = map.end()-1; // Alternative way to get the last element